import { Stack } from 'expo-router';

/** Stack for the Agents tab — active space's automations. */
export default function AgentsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
